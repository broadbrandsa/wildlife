Field-guide statistic: a large serif value with a mono uppercase label. Use in park-fact strips and species vitals.

```jsx
<StatBlock value="19,485 km²" label="Park area" />
<StatBlock value="507" label="Bird species" divider />
```

Use `divider` on all but the first item in a horizontal stat strip. `align="center"` for centered grids.
